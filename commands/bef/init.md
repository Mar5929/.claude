Read the BEF skill file at `skills/bef/SKILL.md` for the full framework context and template references.

Execute the `/bef:init` command as defined in the skill file:

1. Ask for the project name and a one-line description.
2. Create the full `docs/bef/` directory structure as specified in the skill file, including `04-bugs/` and `04-bugs/reports/`.
3. Read the template at `skills/bef/templates/PROJECT_STATE.md` and create the initial `PROJECT_STATE.md` with the project name and description, all stages marked not started, and the Bug Tracking section with empty defaults.
4. Confirm initialization and instruct the user to run `/bef:prd` next.
